% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/travel_time_matrix.R
\name{travel_time_matrix}
\alias{travel_time_matrix}
\title{Calculate travel time matrix between origin destination pairs}
\usage{
travel_time_matrix(
  r5_core,
  origins,
  destinations,
  trip_date,
  departure_time,
  direct_modes = "WALK",
  transit_modes = "TRANSIT",
  max_street_time,
  max_trip_duration = 7200L
)
}
\arguments{
\item{r5_core}{a rJava object to connect with R5 routing engine}

\item{origins}{a spatial sf MULTIPOINT object, or a data.frame containing the
columns 'id', 'lon', 'lat'}

\item{destinations}{a spatial sf MULTIPOINT object, or a data.frame
containing the columns 'id', 'lon', 'lat'}

\item{trip_date}{character string, date in format "yyyy-mm-dd". If working
with public transport networks, check the GTFS.zip
(calendar.txt file) for dates with service.}

\item{departure_time}{character string, time in format "hh:mm:ss"}

\item{direct_modes}{character string, defaults to "WALK". See details for
other options.}

\item{transit_modes}{character string, defaults to "TRANSIT", which includes
all available options of public transport. See details
for options.}

\item{max_street_time}{integer,}

\item{max_trip_duration}{integer, Maximum trip duration in seconds. Defaults
to 7200 seconds (2 hours).}
}
\value{
A data.table with travel-time estimates (in seconds) between origin
destination pairs
}
\description{
Fast function to calculate travel time estimates between one or
multiple origin destination pairs.
}
\details{
R5 allows for multiple combinations of transport modes. The options
include:
\subsection{Transit modes}{

TRAM, SUBWAY, RAIL, BUS, FERRY, CABLE_CAR, GONDOLA, FUNICULAR
}

\subsection{Non transit modes}{

WALK, BICYCLE, CAR, BICYCLE_RENT, CAR_PARK
}
}
\examples{
\donttest{

library(r5r)

# build transport network
path <- system.file("extdata", package = "r5r")
r5_core <- setup_r5(data_path = path)

# load origin/destination points
points <- read.csv(system.file("extdata/poa_hexgrid.csv", package = "r5r"))[1:5,]

# input
direct_modes <- c("WALK", "BICYCLE", "CAR")
transit_modes <-"BUS"
departure_time <- "14:00:00"
trip_date <- "2019-05-20"
street_time = 15L
max_street_time = 30L
max_trip_duration = 300L

df <- travel_time_matrix( r5_core = r5_core,
                          origins = points,
                          destinations = points,
                          trip_date = trip_date,
                          departure_time = departure_time,
                          direct_modes = direct_modes,
                          transit_modes = transit_modes,
                          max_street_time = max_street_time,
                          max_trip_duration = max_trip_duration
                          )

}
}
\seealso{
Other routing: 
\code{\link{detailed_itineraries}()},
\code{\link{multiple_detailed_itineraries}()},
\code{\link{r5_travel_times_from_origin}()}
}
\concept{routing}
